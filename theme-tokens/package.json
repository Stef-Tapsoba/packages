{
    "name": "@myorg/theme-tokens",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "files": [
        "dist",
        "css"
    ],
    "scripts": {
        "build": "npm run generate-css && tsc -b",
        "generate-css": "tsx scripts/generate-css.ts",
        "clean": "rm -rf dist"
    },
    "devDependencies": {
        "tsx": "^4.0.0"
    }
}
