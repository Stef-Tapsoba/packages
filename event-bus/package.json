{
    "name": "@myorg/event-bus",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "files": [
        "dist"
    ],
    "scripts": {
        "build": "tsc -b",
        "clean": "rm -rf dist",
        "test": "vitest",
        "test:watch": "vitest --watch"
    },
    "devDependencies": {
        "vitest": "^1.0.0"
    }
}